workflows:
  default-workflow:
    name: Build Flutter APK
    environment:
      flutter: stable
    scripts:
      - flutter --version
      - flutter pub get
      - flutter build apk
    artifacts:
      - build/app/outputs/flutter-apk/*.apk
